export interface childrenProp {
  children: React.ReactNode;
}

export interface signTypes {
  name?: string;
  phone?: string;
  email: string;
  password: string;
}

export interface setAlertMsgType {
  setAlertMsg: Function;
}
